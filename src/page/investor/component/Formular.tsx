import React from 'react';
import Lists from './formularComponent/Lists';

const Formular = () => {
  console.log(1);
  return (
    <div>
      <Lists />
    </div>
  );
};

export default Formular;
